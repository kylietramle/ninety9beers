<% if activity.trackable && activity.trackable.api_id %>
  <% id = activity.trackable.api_id %>
  <% api_beer = Untappd::Beer.info(id) %>
  made changes to <%= link_to api_beer.beer.beer_name, "/beers/#{id}" %> on the Beer Rack.
<% else %>
   made changes to a beer, which has now been removed.
<% end %>
