<% if activity.trackable && activity.trackable.api_id %>
  <% beer = activity.trackable %>
    added <%= link_to beer.name, "/beers/#{beer.api_id}" %> to the Beer Rack!
<% else %>
   removed one of the beer.
<% end %>
